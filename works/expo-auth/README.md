# expo-auth

展会现场后台的 **认证、RBAC、用户、展会、权限菜单与业务占位壳**。

- 任务：[tasks.yaml](./tasks.yaml)
- 规格：[spec-auth-rbac.md](./spec-auth-rbac.md)

在根目录 [works/registry.yaml](../registry.yaml) 中注册；自动化脚本可只扫 `registry.yaml` 再进入各模块 `tasks.yaml`。
