# API项目


## 目录结构

```text
my-api-project/                     # 项目根目录
├── 📂 supabase/functions/           # Supabase Edge Functions目录 (不可以更改)
│       └── 📂 api/                  # 主API函数（函数名对应URL路径）
│           └── index.ts            # Supabase API入口：https://[project].supabase.co/functions/v1/api/xxx
│
├── 📂 src/                          # 核心业务源代码
│   ├── 📂 config/                   # 应用配置（环境变量、常量）
│   │   ├── app.config.ts           # 应用基础配置（端口、环境等）
│   │   ├── supabase.config.ts      # Supabase连接配置
│   │   ├── auth.config.ts          # JWT、密钥等认证配置
│   │   └── index.ts                # 统一导出所有配置
│   │
│   ├── 📂 controllers/              # 请求控制器（替代原routes目录）
│   │   ├── auth.controller.ts      # 认证相关：登录、注册、退出
│   │   ├── user.controller.ts      # 用户管理：CRUD、资料更新
│   │   ├── product.controller.ts   # 产品管理
│   │   └── index.ts                # 控制器统一导出
│   │
│   ├── 📂 services/                 # 业务逻辑层（核心业务规则）
│   │   ├── auth.service.ts         # 认证业务逻辑
│   │   ├── user.service.ts         # 用户业务逻辑
│   │   └── product.service.ts      # 产品业务逻辑
│   │
│   ├── 📂 middlewares/              # 全局中间件
│   │   ├── auth.middleware.ts      # JWT认证中间件
│   │   ├── validator.middleware.ts # 请求数据验证
│   │   └── logger.middleware.ts    # 请求日志记录
│   │
│   ├── 📂 lib/                      # 工具库和第三方客户端
│   │   ├── supabase.client.ts      # Supabase客户端单例
│   │   ├── logger.ts               # 结构化日志工具
│   │   ├── api-response.ts         # 统一API响应格式
│   │   └── constants.ts            # 全局常量定义
│   │
│   ├── 📂 types/                   # TypeScript类型定义
│   │   ├── user.types.ts           # 用户相关类型
│   │   ├── product.types.ts        # 产品相关类型
│   │   └── api.types.ts            # API通用类型
│   │
│   └── app.ts                      # Hono应用主文件（纯路由定义）
│                                   # 作用：创建Hono实例，注册路由和中间件
│
├── 📂 scripts/                     # 运维和部署脚本
│   ├── deploy.ts                   # 一键部署脚本
│   ├── seed-db.ts                  # 数据库种子数据脚本
│   └── test-local.ts               # 本地测试脚本
│
├── index.ts                        # 项目根入口（本地开发启动）
│                                   # 作用：本地开发服务器入口，调用src/app.ts
│
├── deno.json                       # Deno项目配置文件（任务、导入映射、编译选项）
├── import_map.json                 # 依赖别名映射（简化导入路径）
├── deno.lock                       # 依赖锁定文件（确保版本一致性）
├── .env.example                    # 环境变量示例文件（不含真实密钥）
├── .gitignore                      # Git忽略文件配置
├── README.md                       # 项目说明文档
└── CHANGELOG.md                    # 版本变更记录（可选）
```
