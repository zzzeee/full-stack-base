"use client"

import { Typography } from "antd"

export default function RedeemRecordsMaterialPage() {
    return (
        <div className="panel-surface mx-auto max-w-lg">
            <Typography.Title level={4} style={{ marginTop: 0 }}>
                核销记录 · 物资券
            </Typography.Title>
            <Typography.Text type="secondary">列表与导出将在后续迭代接入。</Typography.Text>
        </div>
    )
}
